// import { useState, useEffect } from 'react';

function App() {

    // const [db, setDb] = useState(4); // Initialize db with the first value of i+2, which is 2+2=4

    // useEffect(() => {
    //   const interval = setInterval(() => {
    //     setDb((prevDb) => prevDb + 1); // Increment db by 1 every second
    //   }, 1000);

    //   return () => clearInterval(interval); // Cleanup the interval on component unmount
    // }, []);

    return <h1>hola | abhinav {'h'}</h1>;
  }


export default App;
